const screens=[...document.querySelectorAll('.screen')];const titles={home:'Inicio',agenda:'Agenda',patients:'Pacientes',invoices:'Facturas',more:'Más',templates:'Plantillas',business:'Mi negocio'};function showScreen(id){screens.forEach(s=>s.classList.toggle('active',s.id===id));document.querySelectorAll('.bottom-nav button').forEach(b=>b.classList.toggle('active',b.dataset.go===id));document.getElementById('screenTitle').textContent=titles[id]||'AutonomIA';window.scrollTo({top:0,behavior:'smooth'})}document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>showScreen(b.dataset.go)));document.getElementById('quickAdd').addEventListener('click',()=>{const a=document.querySelector('.screen.active').id;if(a==='agenda')document.getElementById('appointmentForm').classList.remove('hidden');else{showScreen('agenda');document.getElementById('appointmentForm').classList.remove('hidden')}});document.getElementById('saveAppointment').addEventListener('click',()=>{const p=document.getElementById('apptPatient').value.trim(),t=document.getElementById('apptTime').value,s=document.getElementById('apptService').value,price=document.getElementById('apptPrice').value||'0';if(!p||!t){document.getElementById('apptStatus').textContent='Completa paciente y hora.';return}const r=document.createElement('div');r.className='appointment';r.innerHTML=`<time>${t}</time><div><b>${p}</b><span>${s} · ${price} €</span></div><em>Guardada</em>`;document.getElementById('appointmentList').appendChild(r);document.getElementById('apptStatus').textContent='Cita guardada.'});document.querySelectorAll('.open-patient').forEach(b=>b.addEventListener('click',()=>{document.getElementById('patientDetail').classList.remove('hidden');document.getElementById('patientName').textContent=b.dataset.name;document.getElementById('patientInvoice').dataset.client=b.dataset.name}));document.getElementById('closePatient').addEventListener('click',()=>document.getElementById('patientDetail').classList.add('hidden'));document.getElementById('patientSearch').addEventListener('input',e=>{const term=e.target.value.toLowerCase();document.querySelectorAll('.patient-card').forEach(c=>c.style.display=c.textContent.toLowerCase().includes(term)?'grid':'none')});function updateTotal(){const base=Number(document.getElementById('invoiceBase').value||0),vat=Number(document.getElementById('invoiceVat').value||0);document.getElementById('invoiceTotal').textContent=(base*(1+vat/100)).toLocaleString('es-ES',{style:'currency',currency:'EUR'})}function openInvoice(client='',price='60'){showScreen('invoices');document.getElementById('invoiceForm').classList.remove('hidden');document.getElementById('invoiceClient').value=client;document.getElementById('invoiceBase').value=price;document.getElementById('invoiceDate').valueAsDate=new Date();updateTotal()}document.querySelectorAll('.create-invoice').forEach(b=>b.addEventListener('click',()=>openInvoice(b.dataset.client,b.dataset.price)));document.getElementById('invoiceBase').addEventListener('input',updateTotal);document.getElementById('invoiceVat').addEventListener('change',updateTotal);document.getElementById('saveInvoice').addEventListener('click',()=>alert('Factura guardada en modo demo.'));document.getElementById('printInvoice').addEventListener('click',()=>window.print());document.querySelectorAll('.tabs button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.invoice-card').forEach(c=>c.style.display=b.dataset.filter==='all'||c.dataset.state===b.dataset.filter?'flex':'none')}));const templates={invoice:{s:'Factura de la sesión',b:'Hola [Nombre],\n\nTe adjunto la factura correspondiente a la sesión realizada el [fecha].\n\nUn saludo.'},payment:{s:'Recordatorio de factura pendiente',b:'Hola [Nombre],\n\nTe recuerdo que la factura [número] está pendiente de pago.\n\nGracias.'},appointment:{s:'Recordatorio de tu cita',b:'Hola [Nombre],\n\nTe recuerdo nuestra cita del [fecha] a las [hora].\n\nUn saludo.'}};document.querySelectorAll('.template-btn').forEach(b=>b.addEventListener('click',()=>{const t=templates[b.dataset.template];document.getElementById('templateEditor').classList.remove('hidden');document.getElementById('templateSubject').value=t.s;document.getElementById('templateBody').value=t.b}));document.getElementById('copyTemplate').addEventListener('click',async()=>{const text=`Asunto: ${document.getElementById('templateSubject').value}\n\n${document.getElementById('templateBody').value}`;try{await navigator.clipboard.writeText(text);alert('Texto copiado.')}catch{alert('Selecciona y copia el texto manualmente.')}});if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('sw.js').catch(()=>{}));

document.getElementById('newInvoiceBtn').addEventListener('click',()=>openInvoice());
document.getElementById('savePatient').addEventListener('click',()=>{
  const name=document.getElementById('newPatientName').value.trim();
  const email=document.getElementById('newPatientEmail').value.trim();
  if(!name){document.getElementById('patientStatus').textContent='El nombre es obligatorio.';return;}
  const initials=name.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase();
  const card=document.createElement('article'); card.className='patient-card';
  card.innerHTML=`<div class="avatar">${initials}</div><div><b>${name}</b><span>${email||'Sin email'}</span></div><button class="open-patient" data-name="${name}">›</button>`;
  document.getElementById('patientList').appendChild(card);
  card.querySelector('.open-patient').addEventListener('click',()=>{document.getElementById('patientDetail').classList.remove('hidden');document.getElementById('patientName').textContent=name;document.getElementById('patientInvoice').dataset.client=name;});
  document.getElementById('patientStatus').textContent='Paciente guardado.';
});
document.getElementById('cancelPatient').addEventListener('click',()=>document.getElementById('newPatientForm').classList.add('hidden'));

document.querySelectorAll('.open-resource').forEach(b=>b.addEventListener('click',()=>{document.getElementById('resourceViewer').classList.remove('hidden');document.getElementById('resourceTitle').textContent=b.dataset.title;document.getElementById('resourceContent').textContent=b.dataset.content;}));
document.getElementById('closeResource').addEventListener('click',()=>document.getElementById('resourceViewer').classList.add('hidden'));
